type Playlist {
  id: ID!
  name: String!
  songs: [Song]
}

type Song {
  id: ID!
  title: String!
  artist: String!
  duration: Int!
}

type Query {
  playlist(id: ID!): Playlist
  playlists: [Playlist]
}