export default function Explore() {
  return (
    <div style={{ padding: "40px" }}>
      <h1>Explore Trending Reels</h1>

      <p>
        Server-side rendering (SSR) helps social media applications load
        trending content faster because the HTML is generated on the server
        before reaching the browser. Users receive ready-to-display content,
        improving page load speed and user experience.
      </p>
    </div>
  );
}
